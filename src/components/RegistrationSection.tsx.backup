import React from 'react';
import { CreditCard, Users, Clock, Shield, CheckCircle, Zap } from 'lucide-react';
import { useScrollAnimation } from '../hooks/useScrollAnimation';

const RegistrationSection: React.FC = () => {
  const { isVisible, elementRef } = useScrollAnimation();

  const handleRegistration = () => {
    // This would integrate with Razorpay
    alert('Razorpay integration would be implemented here. Please set up your Razorpay account and keys.');
  };

  return (
    <section id="registration" className="py-24 bg-gradient-to-b from-slate-950 to-indigo-950 relative overflow-hidden">
      {/* Background Effects */}
      <div className="absolute inset-0">
        <div className="absolute top-20 left-20 w-64 h-64 bg-violet-500/10 rounded-full blur-3xl animate-pulse"></div>
        <div className="absolute bottom-20 right-20 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl animate-ping"></div>
      </div>

      <div className="max-w-5xl mx-auto px-6 relative z-10">
        <div 
          ref={elementRef}
          className={`transform transition-all duration-1000 ${
            isVisible ? 'translate-y-0 opacity-100' : 'translate-y-20 opacity-0'
          }`}
        >
          {/* Header */}
          <div className="text-center mb-16">
            <div className="inline-flex items-center space-x-2 bg-red-500/10 border border-red-500/20 rounded-full px-6 py-3 mb-8 animate-pulse">
              <Zap className="w-5 h-5 text-red-400" />
              <span className="text-red-300 font-bold text-lg">⚡ LIMITED SEATS AVAILABLE</span>
            </div>
            
            <h2 className="text-4xl md:text-6xl font-black text-white mb-6">
              Secure Your <span className="text-transparent bg-clip-text bg-gradient-to-r from-violet-400 to-purple-400">Spot</span> Today
            </h2>
            <p className="text-xl text-white/60 max-w-3xl mx-auto">
              Don't miss this opportunity to master networking with industry experts
            </p>
          </div>

          {/* Registration Card */}
          <div className="bg-white/5 backdrop-blur-2xl border border-white/10 rounded-3xl p-12 shadow-2xl">
            {/* Workshop Details */}
            <div className="grid lg:grid-cols-2 gap-12 mb-12">
              <div>
                <h3 className="text-3xl font-bold text-white mb-8 flex items-center">
                  <Clock className="w-8 h-8 text-violet-400 mr-3" />
                  Workshop Details
                </h3>
                <div className="space-y-6">
                  <div className="flex items-center space-x-4 p-4 rounded-xl bg-white/5">
                    <div className="w-12 h-12 bg-gradient-to-br from-violet-500 to-purple-500 rounded-xl flex items-center justify-center">
                      <Clock className="w-6 h-6 text-white" />
                    </div>
                    <div>
                      <div className="text-white font-semibold">September 20th, 2024</div>
                      <div className="text-white/60 text-sm">Mark your calendar</div>
                    </div>
                  </div>
                  <div className="flex items-center space-x-4 p-4 rounded-xl bg-white/5">
                    <div className="w-12 h-12 bg-gradient-to-br from-indigo-500 to-blue-500 rounded-xl flex items-center justify-center">
                      <Users className="w-6 h-6 text-white" />
                    </div>
                    <div>
                      <div className="text-white font-semibold">4-5 Hours Duration</div>
                      <div className="text-white/60 text-sm">Intensive learning session</div>
                    </div>
                  </div>
                  <div className="flex items-center space-x-4 p-4 rounded-xl bg-white/5">
                    <div className="w-12 h-12 bg-gradient-to-br from-emerald-500 to-teal-500 rounded-xl flex items-center justify-center">
                      <Shield className="w-6 h-6 text-white" />
                    </div>
                    <div>
                      <div className="text-white font-semibold">Certificate Included</div>
                      <div className="text-white/60 text-sm">Official LinuxWorld certificate</div>
                    </div>
                  </div>
                </div>
              </div>

              <div>
                <h3 className="text-3xl font-bold text-white mb-8 flex items-center">
                  <CheckCircle className="w-8 h-8 text-emerald-400 mr-3" />
                  What You Get
                </h3>
                <div className="space-y-4">
                  {[
                    'Complete Networking Masterclass',
                    'Hands-on Lab Sessions',
                    'Course Materials & Resources',
                    'LinuxWorld Certificate',
                    'Lifetime Community Access',
                    'Expert Q&A Sessions'
                  ].map((item, index) => (
                    <div key={index} className="flex items-center space-x-4 p-3 rounded-xl bg-white/5 hover:bg-white/10 transition-colors duration-200">
                      <CheckCircle className="w-6 h-6 text-emerald-400 flex-shrink-0" />
                      <span className="text-white font-medium">{item}</span>
                    </div>
                  ))}
                </div>
              </div>
            </div>

            {/* Pricing */}
            <div className="text-center mb-12">
              <div className="bg-gradient-to-r from-violet-500/10 to-purple-500/10 border border-violet-500/20 rounded-3xl p-8">
                <div className="text-6xl font-black text-white mb-4">
                  ₹2,999 
                  <span className="text-2xl text-white/40 line-through ml-4">₹4,999</span>
                </div>
                <div className="text-violet-300 font-bold text-xl mb-2">Early Bird Special - 40% OFF</div>
                <div className="text-white/60">Limited time offer - Ends soon!</div>
              </div>
            </div>

            {/* CTA Button */}
            <div className="text-center mb-8">
              <button 
                onClick={handleRegistration}
                className="group relative bg-gradient-to-r from-violet-600 to-purple-600 hover:from-violet-500 hover:to-purple-500 text-white font-bold py-6 px-12 rounded-2xl text-xl transition-all duration-300 hover:scale-105 hover:shadow-2xl hover:shadow-violet-500/25 w-full md:w-auto min-w-[400px]"
              >
                <div className="flex items-center justify-center space-x-4">
                  <CreditCard className="w-7 h-7" />
                  <span className="text-xl">Register Now - Pay Securely</span>
                </div>
                <div className="absolute inset-0 bg-gradient-to-r from-violet-400 to-purple-400 rounded-2xl opacity-0 group-hover:opacity-20 transition-opacity duration-300"></div>
              </button>
              
              <p className="text-white/60 mt-6 flex items-center justify-center space-x-4">
                <span className="flex items-center space-x-2">
                  <Shield className="w-4 h-4 text-emerald-400" />
                  <span>Secure Payment</span>
                </span>
                <span>•</span>
                <span className="flex items-center space-x-2">
                  <CheckCircle className="w-4 h-4 text-blue-400" />
                  <span>Money-back Guarantee</span>
                </span>
              </p>
            </div>

            {/* Trust Indicators */}
            <div className="grid md:grid-cols-3 gap-6 pt-8 border-t border-white/10">
              {[
                { icon: Shield, title: 'Secure Payment', desc: 'Powered by Razorpay', color: 'text-emerald-400' },
                { icon: Users, title: '15,000+ Alumni', desc: 'Join successful professionals', color: 'text-blue-400' },
                { icon: CheckCircle, title: '4.9/5 Rating', desc: 'Highly rated by students', color: 'text-amber-400' }
              ].map((trust, index) => {
                const IconComponent = trust.icon;
                return (
                  <div key={index} className="text-center p-4 rounded-xl bg-white/5 hover:bg-white/10 transition-colors duration-300">
                    <IconComponent className={`w-8 h-8 ${trust.color} mx-auto mb-3`} />
                    <h4 className="text-white font-semibold mb-1">{trust.title}</h4>
                    <p className="text-white/60 text-sm">{trust.desc}</p>
                  </div>
                );
              })}
            </div>
          </div>

          {/* Urgency Message */}
          <div className="text-center mt-12">
            <div className="inline-flex items-center space-x-3 bg-gradient-to-r from-orange-500/10 to-red-500/10 border border-orange-500/20 rounded-full px-8 py-4 backdrop-blur-sm animate-bounce">
              <div className="w-4 h-4 bg-orange-400 rounded-full animate-ping"></div>
              <span className="text-orange-300 font-bold text-lg">⏰ Only 25 spots remaining - Register before they're gone!</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
};

export default RegistrationSection;